---
- name: Mettre à jour le cache des paquets
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Installer les paquets de base
  apt:
    name:
      - vim
      - curl
      - git
      - ca-certificates
      - apt-transport-https
      - sudo
    state: present
